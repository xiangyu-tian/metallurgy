# P1-W18：113→120 仿真、优化、约束、逐级还原与脱氧实施与准入报告

日期：2026-09-02
基线：`main@60c2232`
范围：`G005、G006、G011、G012、G013、E022、H001`

## 1. 结论

P1-W18 新增并认证 7 项真实可执行工具，注册中心由 113 项闭合至 120 项。动态资格计数为：

```text
registered_count=120
runtime_tool_count=120
catalog_coverage_count=100
qualified_executable_count=120
implementation_qualified_count=120
data_required_count=37
data_qualified_count=37
interface_qualified_count=120
fully_eligible_count=120
```

七项均具有真实实现、严格输入输出 Schema、单位、适用域、标准错误码、来源/版本、依赖/重叠关系、3 个正常用例及 3 个边界或失败用例。每项均可通过统一注册中心、HTTP function-call 接口和固定单工具路由调用。本波未调用外部大模型 API，强制调用测试为本地确定性路由夹具；未生成正式研究数据集，未连接现场传感器，未直接控制设备。

## 2. 七项实现与验证

| ID | 实际能力 | 实现/数据依据 | 独立验证与失败边界 |
| --- | --- | --- | --- |
| G005 | 生成 OpenFOAM v13 `laplacianFoam` 规则长方体导热案例的 6 个必需文件；可只返回清单，也可向项目固定目录生成真实案例、清单文件和确定性 ZIP；不接受任意路径、不执行求解器 | OpenFOAM v13 案例结构、`blockMesh` 与 `laplacianFoam` 约定；Python 标准库安全文件与 ZIP 操作 | 8 顶点、6 面、右手六面体、单元数乘积、网格/场边界一致、目录及 ZIP 逐字节回读、哈希稳定、同输入幂等复用；非法路径名、超限网格及同名异内容关闭式失败且不覆盖 |
| G006 | 对已完全准入的注册工具执行确定性笛卡尔参数批处理 | Python 标准库、统一注册中心 | 计划数等于笛卡尔积，成功结果与直接调用一致，顺序/摘要哈希可复现，成功/失败/重试计数闭合；递归目标、非法路径、超配额失败 |
| G011 | 固定种子的离线 RBF 高斯过程与 Expected Improvement 序贯优化 | Jones–Schonlau–Welch EGO；现有 NumPy/SciPy | 固定种子完全复现、EI 非负、已知目标最优值复核；无可行点、非数值输出、核矩阵数值问题关闭式失败 |
| G012 | 注册工具约束 NSGA-II 多目标优化，输出 Pareto 集、拥挤距离及折中解 | Deb 等 NSGA-II；现有 NumPy | Pareto 解逐对非支配、约束逐项满足、权重归一、固定种子复现；预算越界、目标无穷或全部不可行失败 |
| G013 | 版本化 JSON 规则集的安全表达式树校验 | 白名单算术、聚合、比较与布尔节点；无 `eval`、无模块/文件/网络/数据库访问 | 规则顺序不改变摘要，错误/警告/恒等式计数闭合，违反节点精确定位；未知操作、除零、缺路径、重复 ID、深度/节点超限失败 |
| E022 | 计算 Fe₂O₃→Fe₃O₄→FeO→Fe 的 CO/H₂ 逐级还原 ΔG°、Kp、平衡气比与 Q/K | PostgreSQL 数据集 `DS_NIST_JANAF_IRON_REDUCTION_W18`，NIST WebBook/JANAF 14 条分段 Shomate 记录，500–1100 K | 元素守恒、`K=exp(-ΔG°/RT)`、Q=K 时判为平衡、记录级来源完整；数据库不可用、记录缺失、温区外或分压非正时关闭式失败 |
| H001 | 按调用方显式 K 或 ΔG°及 Wagner 一阶项计算 Al/Si/Mn 脱氧平衡、活度、Q/K 和计量质量 | `DS_IUPAC_AW_2021` 原子量只读查询；Sigworth–Elliott/Wagner 稀溶液方法 | `ΔG°=-RTlnK`、`log10(f)=Σe[%j]`、Q/K=1、脱氧剂/氧化物质量闭合；K/ΔG 二选一、未知元素、数据库不可用或稀溶液超域失败 |

G005、G006、G011、G012、G013 为公式/算法工具。E022、H001 为数据依赖工具，均只读查询指定版本数据，且没有 Python 常数兜底。G006/G011/G012 的嵌套调用目标必须已经 `fully_eligible=true`，并限制递归编排。

## 3. 数据导入与数据库安全

- 新增版本化资产：`Tools/models_core/data/nist_janaf_iron_reduction_w18_v1.json`。
- 数据集：`DS_NIST_JANAF_IRON_REDUCTION_W18`，版本 `2026.09-w18-v1`。
- 资产 SHA-256：`f2458b5414fbe6ed0c3dd4d9cefdb25900cad4b2399087f0c29fc4de62c9a85d`。
- 导入结果：仅追加 1 条 `dataset_registry` 与 14 条 `thermodynamic_correlation`；未 UPDATE、DELETE、TRUNCATE、DROP 旧记录。
- 重复 dry-run：`dataset_inserted=0`、`correlations_inserted=0`、`unchanged=15`、`conflicts=0`，证明导入幂等。
- 导入前数据备份：`backups/p1_w18_pre_nist_janaf_20260902.sql`，不纳入运行时提交。
- 数据库不可用或指定记录缺失时，E022/H001 返回标准数据错误码，不回退到硬编码值。

## 4. 接口、调用与目录

- 七项均已进入统一注册中心，`/api/v1/tools` 返回 120 项并分别给出 function name 与严格 JSON Schema。
- 本地 HTTP 调用及七项逐工具强制路由均通过，验证的是“模型选择已固定后能否调用及正常返回”，不承担自主选型评测。
- 非研究型固定夹具：`Tools/benchmarks/llm_tool_call_cases_p1_w18.json`。
- 目录交叉映射升级为 v22：120 个运行时资产唯一映射；100 项覆盖原目录，20 项为批准的扩展/替代能力。
- Excel 主清单已闭合为 `120 已实现 + 0 后续保留 + 0 新增替换 = 120`，五张表公式错误扫描为 0，并完成保存后全量渲染复核；Markdown 转录同步到相同状态。

## 5. 测试结果

- P1-W18 专项：`13 passed`。覆盖资格计数、科学性质、数据溯源、幂等导入、数据库失败关闭、HTTP、逐工具强制调用，以及 G005 真实目录/ZIP 产出、回读、幂等和冲突保护。
- 全量回归：`314 passed`，耗时 305.34 s；覆盖全部120项工具的历史回归、资格闸门、数据资格、目录交叉映射、HTTP及新增专项。全量运行中 Windows 原生层曾输出一次位于旧 W13 TestClient 线程创建处的 `access violation` 诊断栈，但 pytest 未失败并以 0 退出；涉事用例随后单独复跑为 `1 passed`。
- 现有 `pycalphad` 在 NumPy 2.5 下产生数组 `shape` 赋值弃用警告；这是既有第三方兼容性警告，不影响本轮计算结果或资格计数。

## 6. 依赖、限制与停止条件

- 未新增 Python 依赖；复用现有 Python 标准库、NumPy、SciPy、psycopg2、FastAPI 测试栈。
- 未安装 OpenFOAM。G005 v1.1.0 的真实执行目标是生成经结构校验、落盘回读和 SHA-256 验证的案例输入目录及可选 ZIP；这已经是可读取的真实案例资产，但不包含数值求解结果。是否在安装了 OpenFOAM v13 的独立环境运行 `blockMesh`/`laplacianFoam` 属于后续外部集成。
- G011/G012 为预算受限的离线优化，不是在线控制；G013 只执行安全表达式树；E022 不含非化学计量 FeO/固溶体/动力学；H001 不预测收得率、夹杂上浮或多氧化物固溶。
- 20 项需要现场或训练数据的能力继续停留在延期池，不注册、不生成空 Schema、不计数。
- 120 项总目标达到后停止，不自动新增下一批。若需恢复延期预测能力、接入外部求解器或新增工具，必须由用户另行授权并建立新蓝图。
