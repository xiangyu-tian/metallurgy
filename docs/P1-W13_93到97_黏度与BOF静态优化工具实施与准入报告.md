# P1-W13：93→97 黏度与 BOF 静态优化工具实施与准入报告

## 1. 验收结论

P1-W13 新增并认证 C008、D018、D019、D020 共 4 个独立真实可执行工具。注册中心动态计数为：

| 指标 | 结果 |
| --- | ---: |
| `registered_count` | 97 |
| `qualified_executable_count` | 97 |
| `catalog_coverage_count` | 79 |
| `data_required_count` / `data_qualified_count` | 33 / 33 |
| `interface_qualified_count` | 97 |

四项均具备独立执行实现、严格输入/输出 Schema 与单位、适用域、标准错误码、来源/版本、依赖与重叠关系、至少 3 个正常样例及至少 2 个边界/失败样例，并通过独立数学性质验证和真实 HTTP 强制调用。未调用外部大模型 API，强制调用只使用平台本地隔离实验接口。

## 2. 新增工具与验证依据

| ID | 统一函数名 | 真实实现 | 独立验证 | 主要边界/失败 |
| --- | --- | --- | --- | --- |
| C008 | `metallurgy_estimate_melt_viscosity` | 从 PostgreSQL 读取批准的 Urbain 四元全液态渣关联式或 Hirai 液态金属关联式；Urbain 的原子量也从批准库读取 | 独立重算质量分数→摩尔分数→α→B→A→黏度；同成分升温黏度下降；Hirai 在液相线重现 Andrade 表达式；两温度比满足 Arrhenius 解析式 | Urbain 仅 SiO2-Al2O3-CaO-MgO、1400–2000 K、全液态；Hirai 要求 Tm≤T≤1.5Tm、同一合金基准的密度/液相线/平均摩尔质量；缺数据或超域显式失败 |
| D018 | `metallurgy_optimize_bof_blowing_schedule` | SciPy/HiGHS 线性规划，在固定阶段流量、枪位和时长边界下最小化目标供氧曲线的加权 L1 偏差 | 总氧量闭合；逐段 `duration=volume/flow`；期望曲线可行时目标函数为零；阶段顺序置换不改变按名称映射的解 | 仅离线静态制度，不预测终点 C/T/P、不读取炉气/喷溅信号、不下发设备；阶段容量不可行时失败 |
| D019 | `metallurgy_optimize_alloy_additions` | SciPy/HiGHS LP/MILP，按最终钢液质量基准处理多元素目标、元素收得率、质量保留率、价格和批量约束 | 单合金连续解与闭式质量守恒解一致；逐元素质量账闭合；批量加入量为整数倍 | 不学习历史收得率、不模拟加入顺序；目标不可达、键不一致、保留率不物理或整数约束不可行时失败 |
| D020 | `metallurgy_optimize_coolant_scrap_addition` | 由 `Q=m·cp·ΔT` 构造目标冷却量区间，再用 LP/MILP 求最低成本材料组合 | 单材料解为 `Qmin/q`；独立重构预测终温及能量闭合；单位冷却成本低者优先；批量加入量为整数倍 | 调用方必须给出同一热基准下的有效冷却能力；不内置企业历史物性、不做动态传热；正冷却材料无法升温，热区间不可达时失败 |

## 3. 数据库导入与保护

- 新增版本化资产 `MELT-VISCOSITY-CORRELATIONS-W13-V1`，SHA-256 为 `c57bff170f8f16eccc18ace52d25c2acc62638af58315841e7e5533f798a7048`。
- 导入前生成 PostgreSQL 17 custom-format 备份：`backups/P1-W13_preimport_20260901_01/metallurgy_pre_w13.dump`。
- 备份通过 `PGDMP` 头、`pg_restore --list` 和 SHA-256 校验；备份 SHA-256 为 `05bd3b9c930b3799d25b4dec2e04ab0e0d3e397737b54f3dc07cff815b7be5b5`。
- 迁移 `011_melt_viscosity_correlations.sql` 只新增表和索引；导入器按自然键执行冲突检查，任何内容不一致会回滚整个事务，不更新或删除旧记录。
- 正式导入结果：数据集 1 条、批准模型定义 2 条、审计记录 1 条；`run_id=10a755a2-5938-4128-b2ed-360ad3c8f1b0`。
- 导入后的再次 dry-run 为 `inserted=0、unchanged=3`，数据库仍为 1 个数据集和 2 个模型定义，证明幂等且无重复写入。

备份文件是本机恢复资产，不纳入 Git；迁移、固定数据资产、导入器和只读 Repository 纳入版本控制。

## 4. 注册、目录和接口

- 注册中心显式加载 `models_c_w13`、`models_d_w13`，但计数仍由实现、数据、接口、科学四维闸门动态决定。
- 新增 `tool_catalog_crosswalk_v17.json`，把四项映射为 `same`；运行时资产 97、目录覆盖 79。
- Excel 主目录和 Markdown 镜像已同步到 P1-W13：C008/D018/D019/D020 从“后续保留”移入“已实现”，后续保留由 25 降为 21。
- 规划闭合为 `97 已实现 + 21 后续保留 + 2 新增替换 = 120`；延期现场/训练依赖能力仍为 20，不注册、不计数。

## 5. 测试结果

- P1-W13 专项测试：9/9 通过，覆盖数据库记录溯源、资产哈希、公式/守恒/整数性验证、Schema、真实 HTTP 调用和本地 `forced` 调用。
- 全量回归：214/214 通过，耗时 245.499 秒。
- 每个新工具均有 3 个正常用例、1 个边界用例、2 个失败用例；资格闸门对四项均报告 `normal_cases_passed=3`、`boundary_or_failure_cases_passed=3`。
- 数据导入器再次 dry-run 通过；Python 语法编译通过；工作簿导出后重新导入、公式错误扫描和 5 个工作表视觉核对通过。

## 6. 依赖变化

- 没有安装新依赖，也没有修改依赖锁文件。
- D018～D020 使用仓库现有 NumPy 2.5.1、SciPy 1.18.1 和 HiGHS 后端。
- C008 使用现有 `psycopg2` 数据库访问层和本机 PostgreSQL 17。

## 7. 未解决问题与停止条件

- C008 首版只认证两套公开关联式；Urbain 只覆盖四元全液态渣，Hirai 需要调用方提供同基准液态物性。扩大渣系或增加实验不确定度必须另做数据评审。
- D018 是离线静态分段优化，不能替代在线枪位推荐或终点预测。
- D019 的价格、元素收得率和质量保留率由调用方负责版本一致性；当前没有企业历史标定。
- D020 的有效冷却能力必须包含调用方确认的升温、熔化和反应净效应；当前不与动态热传递模型耦合。
- SciPy/pycalphad 在 NumPy 2.5 下有上游弃用告警，但本轮所有结果和回归均通过；后续依赖升级前应单独验证。

本波到此停止，不自动实现下一批。

## 8. 下一批候选

按当前主目录，下一波可优先评审 E106、E109、E010、E013：完整 Rist 操作线、喷煤等效替代、理论炉顶煤气组成和风口回旋区绝热火焰温度。它们可形成高炉物料/能量链，同时不要求现场时序或训练权重。实施前仍需先提交新一波蓝图，并明确与既有 E006/E009/E012/B012 的范围重叠和数据口径。
