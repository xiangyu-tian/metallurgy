# 末批7项冶金工具可用性人工复核题

日期：2026-09-03

说明：本文只提供适合人工检查工具能否正常工作的业务问题和对应输入，不规定调用模式，不测试工具选择策略。以下输入均已通过注册中心 Schema 校验并实际执行成功。

## G005 OpenFOAM参数化导热案例生成

测试问题：

> 请为案例 `slab_heat_01` 生成一个 OpenFOAM Foundation v13 `laplacianFoam` 导热案例。计算域长、宽、厚分别为 1.0 m、0.2 m、0.1 m，网格为 20×4×2，热扩散率为 1.0×10⁻⁵ m²/s，左右边界温度分别为 1000 K 和 300 K，时间步长 0.1 s，计算至 10 s，每 1 s 写出一次。请生成真实案例目录和 ZIP，但不要执行求解器。

对应输入：

```json
{
  "case_name": "slab_heat_01",
  "length_x_m": 1.0,
  "length_y_m": 0.2,
  "length_z_m": 0.1,
  "cells_x": 20,
  "cells_y": 4,
  "cells_z": 2,
  "diffusivity_m2_s": 0.00001,
  "left_temperature_k": 1000,
  "right_temperature_k": 300,
  "time_step_s": 0.1,
  "end_time_s": 10,
  "write_interval_s": 1,
  "artifact_mode": "directory_and_zip"
}
```

## G006 注册工具参数化仿真批处理

测试问题：

> 请批量调用稳态平板导热工具 T001。平板厚度 0.1 m、面积 1 m²、热端 1000 K、冷端 500 K，分别使用 10、20、40 W/(m·K) 三种热导率计算；最多运行 10 次，不重试，单次失败时继续后续计算。

对应输入：

```json
{
  "target_model_code": "T001",
  "base_params": {
    "thermal_conductivity": 20,
    "thickness": 0.1,
    "area": 1,
    "hot_temperature": 1000,
    "cold_temperature": 500
  },
  "parameter_grid": [
    {
      "path": "thermal_conductivity",
      "values": [10, 20, 40]
    }
  ],
  "max_runs": 10,
  "retry_count": 0,
  "allow_boundary_warning": false,
  "stop_on_failure": false
}
```

## G011 离线高斯过程贝叶斯优化

测试问题：

> 对稳态平板导热工具 T001 做离线贝叶斯优化。平板厚度 0.1 m、面积 1 m²、热端 1000 K、冷端 500 K，在热导率 10～100 W/(m·K) 内最大化热流率 `heat_rate_w`。使用 5 个初始样本，总评估预算 9，随机种子 11，Expected Improvement 探索量 0.01。

对应输入：

```json
{
  "target_model_code": "T001",
  "base_params": {
    "thermal_conductivity": 20,
    "thickness": 0.1,
    "area": 1,
    "hot_temperature": 1000,
    "cold_temperature": 500
  },
  "variables": [
    {
      "path": "thermal_conductivity",
      "lower_bound": 10,
      "upper_bound": 100
    }
  ],
  "objective_output_path": "heat_rate_w",
  "objective_sense": "maximize",
  "constraints": [],
  "initial_samples": 5,
  "evaluation_budget": 9,
  "seed": 11,
  "expected_improvement_xi": 0.01,
  "allow_boundary_warning": false
}
```

## G012 注册工具NSGA-II多目标优化

测试问题：

> 对稳态平板导热工具 T001 做多目标优化。平板厚度 0.1 m、面积 1 m²、热端 1000 K、冷端 500 K，在热导率 10～100 W/(m·K) 内，同时最大化热流率 `heat_rate_w` 和热阻 `thermal_resistance_k_per_w`，两项目标权重均为 0.5。使用种群 10、迭代 3 代、随机种子 5。

对应输入：

```json
{
  "target_model_code": "T001",
  "base_params": {
    "thermal_conductivity": 20,
    "thickness": 0.1,
    "area": 1,
    "hot_temperature": 1000,
    "cold_temperature": 500
  },
  "variables": [
    {
      "path": "thermal_conductivity",
      "lower_bound": 10,
      "upper_bound": 100
    }
  ],
  "objectives": [
    {
      "name": "heat",
      "output_path": "heat_rate_w",
      "sense": "maximize"
    },
    {
      "name": "resistance",
      "output_path": "thermal_resistance_k_per_w",
      "sense": "maximize"
    }
  ],
  "constraints": [],
  "population_size": 10,
  "generations": 3,
  "seed": 5,
  "compromise_weights": [0.5, 0.5],
  "allow_boundary_warning": false
}
```

## G013 安全约束规则校验器

测试问题：

> 对一次物料平衡结果执行规则校验：输入质量为 100 kg，输出质量为 99.99 kg，要求两者绝对残差不超过 0.02 kg；若违反则提示检查物流。

对应输入：

```json
{
  "rule_set_id": "mass_balance",
  "rule_set_version": "1",
  "context": {
    "input": 100,
    "output": 99.99
  },
  "rules": [
    {
      "id": "closure",
      "description": "质量残差不超过0.02",
      "severity": "error",
      "assertion": {
        "op": "within",
        "args": [
          {"path": "input"},
          {"path": "output"},
          {"constant": 0.02}
        ]
      },
      "recommendation": "检查物流"
    }
  ]
}
```

## E022 铁氧化物逐级还原平衡

测试问题：

> 计算 800 K 下赤铁矿 Fe₂O₃ 向磁铁矿 Fe₃O₄ 的 CO 逐级还原平衡。CO 分压为 0.7 bar，CO₂ 分压为 0.3 bar；请给出标准 Gibbs 能、平衡常数、实际 Q/K 和反应方向。

对应输入：

```json
{
  "reaction_stage": "hematite_to_magnetite",
  "reducing_gas": "CO",
  "temperature_k": 800,
  "reducing_gas_partial_pressure_bar": 0.7,
  "oxidized_gas_partial_pressure_bar": 0.3
}
```

## H001 显式参数钢液脱氧平衡

测试问题：

> 对 100000 kg 钢液计算铝脱氧平衡。温度为 1873 K，钢液中铝为 0.05 wt%，反应平衡常数为 1.0×10¹²，Al₂O₃ 活度为 1，采用理想稀溶液且不设置 Wagner 交互项；求平衡氧含量及脱氧计量结果。

对应输入：

```json
{
  "deoxidizer": "Al",
  "temperature_k": 1873,
  "calculation_mode": "equilibrium_oxygen",
  "equilibrium_constant": 1000000000000.0,
  "deoxidizer_mass_percent": 0.05,
  "oxide_activity": 1.0,
  "interaction_terms": [],
  "steel_mass_kg": 100000
}
```
